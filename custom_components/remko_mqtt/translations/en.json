{
  "title": "Remko MQTT",
  "config": {
    "flow_title": "Flow title",
    "abort": {
      "already_configured": "The specified ID is already in use."
    },
    "error": {
      "invalid_nodename": "The provided MQTT node name is invalid",
      "creation_id": "The ID is not unique or valid",
      "creation_error": "An error occurred while creating the configuration",
      "invalid_language": "Invalid language selected"
    },
    "step": {
      "user": {
        "data": {
          "id_name": "Unique ID",
          "model": "Heat Pump Model",
          "mqtt_node": "MQTT Node Name",
          "language": "Language",
          "freq": "Maximum Update Interval (in seconds)"
        },
        "title": "Heat Pump Configuration",
        "description": "Create a new Remko_MQTT instance"
      }
    },
    "progress": {
      "slow_task": "This message will be displayed if `slow_task` is returned as `progress_action` for `async_show_progress`."
    }
  },
  "options": {
    "abort": {
      "already_configured": "The specified ID is already in use."
    },
    "step": {
      "user": {
        "data": {
          "model": "Heat Pump Model",
          "mqtt_node": "MQTT Node Name",
          "language": "Language",
          "freq": "Maximum Update Interval (in seconds)"
        },
        "title": "Options"
      }
    }
  }
}